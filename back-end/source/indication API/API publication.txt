# 📡 API Publications — Documentation Frontend

> **Base URL :** `http://localhost:8000/api/publications/`  
> **Auth :** `Authorization: Bearer <access_token>` (uniquement sur les endpoints qui le requièrent)  
> **Pagination :** tous les endpoints liste supportent `?page=1&page_size=12`

---

## Sommaire

- [Liste des publications](#1-liste-des-publications)
- [Détail d'une publication](#2-détail-dune-publication)
- [Créer une publication](#3-créer-une-publication-photographe)
- [Modifier une publication](#4-modifier-une-publication-photographe-propriétaire)
- [Supprimer une publication](#5-supprimer-une-publication-photographe-propriétaire)
- [Like / Unlike](#6-like--unlike)
- [Mes publications](#7-mes-publications-photographe)
- [Catégories](#8-catégories)
- [Tags — autocomplétion](#9-tags--autocomplétion)
- [Filtres et recherche](#filtres-et-recherche)
- [Droits de publication selon abonnement](#droits-de-publication-selon-abonnement)
- [Structure des objets](#structure-des-objets)

---

## 1. Liste des publications

**`GET /api/publications/`** — Public, pas de token requis

### Réponse `200 OK`

```json
{
  "success": true,
  "pagination": {
    "total": 48,
    "pages": 4,
    "page_actuelle": 1,
    "page_size": 12,
    "suivante": "http://localhost:8000/api/publications/?page=2",
    "precedente": null
  },
  "data": [ /* liste de publications */ ]
}
```

---

## 2. Détail d'une publication

**`GET /api/publications/<uuid>/`** — Public

### Réponse `200 OK`

```json
{
  "success": true,
  "data": {
    "id": "...",
    "titre": "Coucher de soleil sur les Calanques",
    "description": "Prise à Marseille en juillet.",
    "image_affichee": "http://localhost:8000/media/publications/filigranes/wm_photo.jpg",
    "est_vendue": false,
    "type": "vente",
    "prix": "120.00",
    "categories": [{ "id": "...", "nom": "Nature", "description": null }],
    "tags": [{ "id": "...", "nom": "golden-hour" }],
    "nombre_likes": 34,
    "a_like": false,
    "photographe_nom": "Marie Martin",
    "photographe_photo": "http://localhost:8000/media/profils/marie.jpg",
    "photographe_id": "...",
    "is_active": true,
    "created_at": "2024-11-15T10:30:00Z",
    "updated_at": "2024-11-15T11:00:00Z"
  }
}
```

> 💡 **`image_affichee`** est l'unique champ image à utiliser côté React :
> - Tant que `est_vendue = false` → c'est la version **avec filigrane**
> - Quand `est_vendue = true` → c'est automatiquement la version **originale sans filigrane**
>
> Tu n'as pas à gérer cette logique côté frontend — le backend s'en occupe.

---

## 3. Créer une publication *(photographe)*

**`POST /api/publications/`** — 🔒 Photographe requis  
**Content-Type :** `multipart/form-data`

> ⚠️ **Le photographe doit avoir un abonnement actif.** Voir [droits selon abonnement](#droits-de-publication-selon-abonnement).

| Champ | Type | Requis | Description |
|---|---|---|---|
| `titre` | string | ✅ | |
| `description` | string | ❌ | |
| `image_originale` | file | ✅ | L'image de la publication |
| `type` | string | ❌ | `"publicite"` (défaut) ou `"vente"` |
| `prix` | number | ⚠️ | Obligatoire si `type = "vente"` |
| `categories` | UUID[] | ❌ | Liste d'UUIDs de catégories existantes |
| `tags` | string[] | ❌ | Liste de noms de tags |
| `is_active` | boolean | ❌ | `true` par défaut |

> ⚠️ **Envoi des listes en FormData :**
> ```javascript
> formData.append("categories", "uuid-1");
> formData.append("tags", "golden-hour");
> formData.append("tags", "nature");
> ```

> 🎨 Le filigrane est généré **automatiquement** côté serveur à l'upload. Tu envoies uniquement `image_originale`.

### Réponse `201 Created`

```json
{
  "success": true,
  "message": "Publication créée.",
  "data": { /* PublicationDetail */ }
}
```

### Réponses erreur

```json
{ "success": false, "errors": "Vous devez avoir un abonnement actif pour publier." }
{ "success": false, "errors": "La publication de type 'vente' est réservée aux abonnés Premium." }
{ "success": false, "errors": { "prix": ["Le prix est obligatoire pour une publication de type vente."] } }
```

---

## 4. Modifier une publication *(photographe propriétaire)*

**`PATCH /api/publications/<uuid>/`** — 🔒 Propriétaire uniquement  
Tous les champs sont optionnels.

> 💡 Si une nouvelle `image_originale` est envoyée, le filigrane est automatiquement régénéré.

### Réponse `200 OK`

```json
{
  "success": true,
  "message": "Publication mise à jour.",
  "data": { /* PublicationDetail */ }
}
```

---

## 5. Supprimer une publication *(photographe propriétaire)*

**`DELETE /api/publications/<uuid>/`** — 🔒 Propriétaire uniquement

### Réponse `200 OK`

```json
{
  "success": true,
  "message": "Publication supprimée."
}
```

---

## 6. Like / Unlike

**`POST /api/publications/<uuid>/like/`** — 🔒 Authentifié requis

### Réponse `200 OK`

```json
{
  "success": true,
  "data": {
    "liked": true,
    "nombre_likes": 35
  }
}
```

---

## 7. Mes publications *(photographe)*

**`GET /api/publications/mes-publications/`** — 🔒 Photographe requis  
Retourne toutes les publications du photographe connecté, actives ET inactives.

---

## 8. Catégories

### Lister — `GET /api/publications/categories/` — Public

```json
{
  "success": true,
  "data": [
    { "id": "...", "nom": "Portrait", "description": null },
    { "id": "...", "nom": "Mariage", "description": "Photos de mariages" }
  ]
}
```

### Créer — `POST /api/publications/categories/` — 🔒 Admin uniquement

```json
{ "nom": "Architecture", "description": "Photographie de bâtiments" }
```

---

## 9. Tags — autocomplétion

**`GET /api/publications/tags/?q=<terme>`** — Public, max 10 résultats

```json
{
  "success": true,
  "data": [
    { "id": "...", "nom": "golden-hour" },
    { "id": "...", "nom": "gold-coast" }
  ]
}
```

---

## Filtres et recherche

| Paramètre | Valeur | Exemple |
|---|---|---|
| `type` | `publicite` ou `vente` | `?type=vente` |
| `categorie` | UUID d'une catégorie | `?categorie=<uuid>` |
| `tag` | nom du tag | `?tag=golden-hour` |
| `photographe` | UUID du profil photographe | `?photographe=<uuid>` |
| `q` | recherche libre | `?q=mariage+paris` |
| `page` | numéro de page | `?page=2` |
| `page_size` | nombre par page (max 50) | `?page_size=24` |

---

## Droits de publication selon abonnement

| Abonnement | `publicite` | `vente` |
|---|---|---|
| Aucun / expiré | ❌ | ❌ |
| Basic | ✅ | ❌ |
| Premium | ✅ | ✅ |

> Si le photographe n'a pas d'abonnement actif, l'API retourne `400` avec :
> ```json
> { "success": false, "errors": "Vous devez avoir un abonnement actif pour publier." }
> ```

---

## Structure des objets

```typescript
interface Publication {
  id: string;
  titre: string;
  description: string | null;
  image_affichee: string;   // URL unique — filigrane OU originale selon est_vendue
  est_vendue: boolean;      // true = photo achetée, image_affichee = originale sans filigrane
  type: "publicite" | "vente";
  prix: string | null;
  categories: Categorie[];
  tags: Tag[];
  nombre_likes: number;
  a_like: boolean;
  photographe_nom: string;
  photographe_photo: string | null;
  photographe_id: string;   // détail uniquement
  is_active: boolean;
  created_at: string;
  updated_at: string;       // détail uniquement
}

interface Categorie {
  id: string;
  nom: string;
  description: string | null;
}

interface Tag {
  id: string;
  nom: string;
}
```

---

*Documentation — module publications. Voir aussi `README_API_AUTH.md`, `README_API_PROFIL.md`, `README_API_ABONNEMENTS.md`.*